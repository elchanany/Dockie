# Android
*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
app/build/
app/release/

# Signing keystores and secrets — never commit
*.jks
*.keystore
*.p12
*.pfx
signing/
release.keystore
dockie-release.keystore
**/keystore.properties
gradle.properties.local

# Release artifact staged at repo root for convenience (regenerated by CI)
/Dockie.apk

# Google Play publisher credentials + local AAB (never commit)
play-service-account.json
*.aab

# OS / editor noise
Thumbs.db
*.swp
hs_err_pid*.log
replay_pid*.log

# Local agent runtime (not shared protocol)
.codex/

# Graphify: keep shared graph; ignore runtime caches only
graphify-out/cost.json
graphify-out/.graphify_cache/
graphify-out/**/cache/


graphify-out/20*/

