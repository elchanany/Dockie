# Android
*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
app/build/
app/release/

# Signing keystores and secrets — never commit
*.jks
*.keystore
*.p12
*.pfx
signing/
release.keystore
dockie-release.keystore
**/keystore.properties
gradle.properties.local

# Release artifact staged at repo root for convenience (regenerated by CI)
/Dockie.apk

# Google Play publisher credentials + local AAB (never commit)
play-service-account.json
*.aab

# OS / editor noise
Thumbs.db
*.swp

# Local agent/knowledge-graph artifacts (environment-specific, never publish)
.codex/
graphify-out/
AGENTS.md
.gitattributes
